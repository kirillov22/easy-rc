{
  "name": "easy-rc-client",
  "private": true,
  "type": "module",
  "scripts": {
    "proto": "pbjs -t static-module -w es6 --force-number --no-delimited --no-convert --no-verify -o src/generated/messages.js ../common/messages.proto && pbts -o src/generated/messages.d.ts src/generated/messages.js",
    "dev": "vite",
    "build": "tsc && vite build",
    "test": "vitest run",
    "lint": "eslint src/"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.2.0",
    "jsdom": "^29.0.2",
    "protobufjs-cli": "^2.0.1",
    "typescript": "^6.0.2",
    "typescript-eslint": "^8.58.1",
    "vite": "^8.0.7",
    "vitest": "^4.1.3"
  },
  "dependencies": {
    "protobufjs": "^8.0.1"
  }
}
